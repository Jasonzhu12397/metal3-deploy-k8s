# Base URL the browser calls for the API. Leave empty in dev (Vite proxies
# /api to VITE_API_PROXY_TARGET, see vite.config.ts) -- only set this for a
# production build where the frontend is served separately from the API.
VITE_API_BASE_URL=
