-- Placeholder for any bootstrap SQL (extensions, roles) beyond what
-- SQLAlchemy's create_all() handles. Alembic migrations should be used
-- for anything beyond initial dev setup.
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
